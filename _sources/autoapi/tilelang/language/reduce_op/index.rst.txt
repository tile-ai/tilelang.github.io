tilelang.language.reduce_op
===========================

.. py:module:: tilelang.language.reduce_op

.. autoapi-nested-parse::

   Reduce operations exposed on the TileLang language surface.



Attributes
----------

.. autoapisummary::

   tilelang.language.reduce_op.ReduceKind


Functions
---------

.. autoapisummary::

   tilelang.language.reduce_op.reduce
   tilelang.language.reduce_op.reduce_max
   tilelang.language.reduce_op.reduce_min
   tilelang.language.reduce_op.reduce_sum
   tilelang.language.reduce_op.reduce_abssum
   tilelang.language.reduce_op.reduce_absmax
   tilelang.language.reduce_op.reduce_bitand
   tilelang.language.reduce_op.reduce_bitor
   tilelang.language.reduce_op.reduce_bitxor
   tilelang.language.reduce_op.reducer_init
   tilelang.language.reduce_op.reducer_update
   tilelang.language.reduce_op.finalize_reducer
   tilelang.language.reduce_op.warp_reduce_sum
   tilelang.language.reduce_op.warp_reduce_max
   tilelang.language.reduce_op.warp_reduce_min
   tilelang.language.reduce_op.warp_reduce_bitand
   tilelang.language.reduce_op.warp_reduce_bitor


Module Contents
---------------

.. py:data:: ReduceKind

.. py:function:: reduce(buffer, out, reduce_type, dim, clear, batch = 1, nan_propagate = False, annotations = None)

   Perform a reduction operation on a buffer along a specified dimension.

   :param buffer: Input buffer to reduce
   :type buffer: tirx.Buffer
   :param out: Output buffer to store results
   :type out: tirx.Buffer
   :param reduce_type: Type of reduction ('max', 'min', 'sum', 'abssum')
   :type reduce_type: str
   :param dim: Dimension along which to perform reduction
   :type dim: int
   :param clear: Whether to initialize the output buffer before reduction
   :type clear: bool
   :param batch: Number of output elements per batched AllReduce call
                 (default 1 = scalar, current behaviour). When batch > 1 the
                 compiler emits ceil(N/batch) batched AllReduce calls each sharing
                 a single pair of barriers, reducing total barrier count by batch×.
                 batch must evenly divide the per-thread output element count N.
   :type batch: int
   :param nan_propagate: Only meaningful for max/min/absmax on
                         float16/bfloat16. When True, lower to CUDA __hmax_nan/__hmin_nan so
                         NaNs propagate through the reduction. When False (default), use
                         __hmax/__hmin which return the non-NaN operand. CUDA-only.
   :type nan_propagate: bool


.. py:function:: reduce_max(buffer, out, dim = -1, clear = True, batch = 1, nan_propagate = False, annotations = None)

   Perform reduce max on input buffer, store the result to output buffer

   :param buffer: The input buffer.
   :type buffer: Buffer
   :param out: The output buffer.
   :type out: Buffer
   :param dim: The dimension to perform reduce on
   :type dim: int
   :param clear: If set to True, the output buffer will first be initialized to -inf.
   :type clear: bool
   :param batch: Number of output elements per batched AllReduce call (default 1).
   :type batch: int
   :param nan_propagate: For float16/bfloat16 only. When True, NaN inputs propagate through the
                         reduction (CUDA __hmax_nan). When False (default), NaN inputs are
                         ignored in favor of the other operand (CUDA __hmax). CUDA-only.
   :type nan_propagate: bool

   :returns: **handle**
   :rtype: PrimExpr


.. py:function:: reduce_min(buffer, out, dim = -1, clear = True, batch = 1, nan_propagate = False, annotations = None)

   Perform reduce min on input buffer, store the result to output buffer.

   :param buffer: The input buffer
   :type buffer: tirx.Buffer
   :param out: The output buffer
   :type out: tirx.Buffer
   :param dim: The dimension to perform reduce on
   :type dim: int
   :param clear: If True, output buffer will be initialized to inf. Defaults to True.
   :type clear: bool, optional
   :param batch: Number of output elements per batched AllReduce call (default 1).
   :type batch: int
   :param nan_propagate: For float16/bfloat16 only. When True,
                         NaN inputs propagate (CUDA __hmin_nan). When False (default), NaNs
                         are ignored (CUDA __hmin). CUDA-only.
   :type nan_propagate: bool, optional

   :returns: Handle to the reduction operation
   :rtype: tirx.Call


.. py:function:: reduce_sum(buffer, out, dim = -1, clear = True, batch = 1, annotations = None)

   Perform reduce sum on input buffer, store the result to output buffer.

   :param buffer: The input buffer
   :type buffer: tirx.Buffer
   :param out: The output buffer
   :type out: tirx.Buffer
   :param dim: The dimension to perform reduce on
   :type dim: int
   :param clear: If True, output buffer will be cleared before reduction.
                 If False, results will be accumulated on existing values.
                 Defaults to True.
   :type clear: bool, optional
   :param batch: Number of output elements per batched AllReduce call (default 1).
   :type batch: int

   Note: When clear=True, reduce_sum will not compute directly on the output buffer. This is because
         during warp reduction, the same value would be accumulated multiple times (number of threads
         in the warp). Therefore, the implementation with clear=True follows these steps:
       1. create a temp buffer with same shape and dtype as out
       2. copy out to temp buffer
       3. call reduce_sum with temp buffer and out
       4. Add temp buffer to out

   :returns: Handle to the reduction operation
   :rtype: tirx.Call


.. py:function:: reduce_abssum(buffer, out, dim = -1, batch = 1, annotations = None)

   Perform reduce absolute sum on input buffer, store the result to output buffer.

   :param buffer: The input buffer
   :type buffer: tirx.Buffer
   :param out: The output buffer
   :type out: tirx.Buffer
   :param dim: The dimension to perform reduce on
   :type dim: int
   :param batch: Number of output elements per batched AllReduce call (default 1).
   :type batch: int

   :returns: Handle to the reduction operation
   :rtype: tirx.Call


.. py:function:: reduce_absmax(buffer, out, dim = -1, clear = True, batch = 1, nan_propagate = False, annotations = None)

   Perform reduce absolute max on input buffer, store the result to output buffer.

   :param buffer: The input buffer
   :type buffer: tirx.Buffer
   :param out: The output buffer
   :type out: tirx.Buffer
   :param dim: The dimension to perform reduce on
   :type dim: int
   :param batch: Number of output elements per batched AllReduce call (default 1).
   :type batch: int
   :param nan_propagate: For float16/bfloat16 only. When True,
                         NaN inputs propagate (CUDA __hmax_nan). When False (default), NaNs
                         are ignored. CUDA-only.
   :type nan_propagate: bool, optional

   :returns: Handle to the reduction operation
   :rtype: tirx.Call


.. py:function:: reduce_bitand(buffer, out, dim = -1, clear = True, batch = 1, annotations = None)

   Perform reduce bitwise-and on input buffer, store the result to output buffer.

   :param buffer: The input buffer
   :type buffer: tirx.Buffer
   :param out: The output buffer
   :type out: tirx.Buffer
   :param dim: The dimension to perform reduce on
   :type dim: int
   :param batch: Number of output elements per batched AllReduce call (default 1).
   :type batch: int

   :returns: Handle to the reduction operation
   :rtype: tirx.Call


.. py:function:: reduce_bitor(buffer, out, dim = -1, clear = True, batch = 1, annotations = None)

   Perform reduce bitwise-or on input buffer, store the result to output buffer.

   :param buffer: The input buffer
   :type buffer: tirx.Buffer
   :param out: The output buffer
   :type out: tirx.Buffer
   :param dim: The dimension to perform reduce on
   :type dim: int
   :param batch: Number of output elements per batched AllReduce call (default 1).
   :type batch: int

   :returns: Handle to the reduction operation
   :rtype: tirx.Call


.. py:function:: reduce_bitxor(buffer, out, dim = -1, clear = True, batch = 1, annotations = None)

   Perform reduce bitwise-xor on input buffer, store the result to output buffer.

   :param buffer: The input buffer
   :type buffer: tirx.Buffer
   :param out: The output buffer
   :type out: tirx.Buffer
   :param dim: The dimension to perform reduce on
   :type dim: int

   :returns: Handle to the reduction operation
   :rtype: tirx.Call


.. py:function:: reducer_init(reducer, init=None)

   Open a reducer epoch, optionally with a logical starting value.

   Must appear exactly once per `T.alloc_reducer` allocation, before any
   `T.reducer_update`. The whole epoch (init, updates, finalize) may sit
   inside thread-uniform serial loops or conditionals — the epoch then
   reopens once per dynamic execution — but init and finalize must share
   the same enclosing loop/branch scope. Without `init`, the reduction
   starts from the combine identity (sum -> 0, max -> dtype lowest, min ->
   dtype highest, bitand -> all ones, bitor/bitxor -> 0).

   `init` is a LOGICAL starting value: the result is as if one extra
   contribution `init` were combined into every logical output, exactly
   once. It is not a physical fill — physical partials always start from
   the identity, and the compiler captures `init` at the init site and
   combines it once per logical output at finalize time, so physical
   replication can never multiply it and later writes to buffers the
   expression reads cannot change the epoch's starting value.

   :param reducer: Handle returned by `T.alloc_reducer`.
   :type reducer: tirx.Buffer
   :param init: Optional logical starting
                value; converted to the reducer's dtype when given as a Python
                number.
   :type init: PrimExpr | int | float | None

   :returns: Handle to the reducer_init intrinsic call.
   :rtype: tirx.Call


.. py:function:: reducer_update(target, value)

   Contribute `value` to one logical output of a reducer.

   `target` must be written as `acc[indices]` directly in the first argument
   position; it is an update-target descriptor, not a read of the reducer's
   current value. Each dynamic logical iteration of the enclosing
   `T.Parallel` loop contributes exactly once, regardless of how the loop is
   physically replicated over threads.

   :param target: `acc[indices]` selecting the logical output.
   :type target: tirx.BufferLoad
   :param value: Contribution expression (cast to the reducer dtype if needed).

   :returns: Handle to the reducer_update intrinsic call.
   :rtype: tirx.Call


.. py:function:: finalize_reducer(reducer, dst = None, batch = 1, annotations = None)

   Close a reducer epoch.

   v2 form (``dst`` given): complete the cross-participant communication the
   chosen physical plan requires, combine the optional ``T.reducer_init``
   starting value exactly once per logical output, and write the logical
   result into the independent destination fragment ``dst``. After this
   call the reducer handle is dead; read results from ``dst``.

   Legacy v1 form (``dst`` omitted): in-place finalize of a legacy
   ``alloc_reducer(replication=...)`` fragment reducer. Deprecated.

   :param reducer: Reducer handle.
   :type reducer: tirx.Buffer
   :param dst: Destination fragment (v2). Same logical
               shape and dtype as the reducer.
   :type dst: tirx.Buffer | None
   :param batch: Batched AllReduce width: the collective covers `batch`
                 output elements per call, sharing one pair of barriers.
   :type batch: int

   :returns: Handle to the finalize intrinsic call.
   :rtype: tirx.Call


.. py:function:: warp_reduce_sum(value)

   Perform warp reduction sum on a register value.

   This function reduces a value across all threads in a warp using shuffle operations.
   Each thread provides a  register `value`, and after the reduction, all threads
   will have the sum of all values across the warp.

   :param value: The input register value to reduce
   :type value: tirx.PrimExpr

   :returns: The reduced sum value (same on all threads in the warp)
   :rtype: tirx.PrimExpr


.. py:function:: warp_reduce_max(value)

   Perform warp reduction max on a register value.

   This function reduces a value across all threads in a warp using shuffle operations.
   Each thread provides a  register `value`, and after the reduction, all threads
   will have the max of all values across the warp.

   :param value: The input register value to reduce
   :type value: tirx.PrimExpr

   :returns: The reduced max value (same on all threads in the warp)
   :rtype: tirx.PrimExpr


.. py:function:: warp_reduce_min(value)

   Perform warp reduction min on a register value.

   This function reduces a value across all threads in a warp using shuffle operations.
   Each thread provides a  register `value`, and after the reduction, all threads
   will have the min of all values across the warp.

   :param value: The input register value to reduce
   :type value: tirx.PrimExpr

   :returns: The reduced min value (same on all threads in the warp)
   :rtype: tirx.PrimExpr


.. py:function:: warp_reduce_bitand(value)

   Perform warp reduction bitwise-and on a register value.

   This function reduces a value across all threads in a warp using shuffle operations.
   Each thread provides a  register `value`, and after the reduction, all threads
   will have the bitwise-and of all values across the warp.

   :param value: The input register value to reduce
   :type value: tirx.PrimExpr

   :returns: The reduced bitwise-and value (same on all threads in the warp)
   :rtype: tirx.PrimExpr


.. py:function:: warp_reduce_bitor(value)

   Perform warp reduction bitwise-or on a register value.

   This function reduces a value across all threads in a warp using shuffle operations.
   Each thread provides a  register `value`, and after the reduction, all threads
   will have the bitwise-or of all values across the warp.

   :param value: The input register value to reduce
   :type value: tirx.PrimExpr

   :returns: The reduced bitwise-or value (same on all threads in the warp)
   :rtype: tirx.PrimExpr


